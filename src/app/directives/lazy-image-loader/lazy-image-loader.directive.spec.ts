import { LazyImageLoaderDirective } from './lazy-image-loader.directive';

describe('LazyImageLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyImageLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
